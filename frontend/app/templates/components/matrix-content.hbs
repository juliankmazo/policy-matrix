<div {{action "toggleBody"}}>
  {{#if isShowingBody}}
    {{truncate-text body}}
  {{else}}
    {{#if acronym}}
      {{acronym}}
    {{else}}
    	{{truncate-text body limit=5}}
    {{/if}}
  {{/if}}
</div>
{{yield}}
